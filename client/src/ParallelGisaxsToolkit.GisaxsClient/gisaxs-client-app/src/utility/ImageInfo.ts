class ImageInfo {
    id: number;
    width: number;
    height: number;
    constructor(id: number, width: number, height: number)
    {
        this.id = id;
        this.height = height;
        this.width = width;
    }
}

export { ImageInfo }